{
  "name": "chatgpt-web-cli",
  "version": "0.1.0",
  "description": "A terminal client for continuing real ChatGPT web conversations through a persistent browser session.",
  "type": "module",
  "bin": {
    "chatgpt-web": "src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "check": "node --check src/cli.js && node --check src/browser.js && node --check src/chatgpt.js && node --check src/chrome.js && node --check src/terminal.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright-core": "^1.55.0"
  }
}
